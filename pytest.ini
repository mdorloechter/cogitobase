[pytest]
pythonpath = .
asyncio_mode = auto
# pytest-asyncio 1.x: pin the fixture loop scope explicitly so the default can't
# shift under us on a future release (silences the deprecation nudge).
asyncio_default_fixture_loop_scope = function
testpaths = tests
